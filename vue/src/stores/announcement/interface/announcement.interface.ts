export interface AnnouncementRequest {
    title: string;
    details: string;
    is_active: boolean;
}
export interface Announcement {
    id: number;
    title: string;
    details: string;
    is_active: boolean;
}
